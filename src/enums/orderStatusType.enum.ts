export enum OrderStatusType {
  PENDING = 'pending',
  DELIVERED = 'delivered',
  CANCELLED = 'cancelled',
  DECLINED = 'declined',
  REFUNDED = 'refunded',
  SHIPPED = 'shipped',
  COMPLETED = 'completed',
  PAID = 'paid',
}
